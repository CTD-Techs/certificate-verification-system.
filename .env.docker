# Docker Compose Environment Variables for AWS RDS Deployment
# Copy this file to .env and update with your actual AWS RDS values

# Node Environment
NODE_ENV=production

# AWS RDS PostgreSQL Configuration
# Replace with your actual RDS endpoint from AWS Console
# Format: your-db-instance.xxxxxxxxxx.region.rds.amazonaws.com
# Example: cert-verify-db.c9akciq32.us-east-1.rds.amazonaws.com
DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_secure_rds_password_change_this
DB_NAME=cert_verification

# Backend Configuration
PORT=3000

# JWT Configuration
# IMPORTANT: Generate a secure random string (minimum 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your_very_secure_jwt_secret_min_32_characters_change_this
JWT_EXPIRES_IN=7d

# Security
BCRYPT_ROUNDS=10

# CORS Configuration
# Replace with your EC2 instance public IP or domain
CORS_ORIGIN=http://your-ec2-ip:5151

# Frontend Configuration
FRONTEND_PORT=80
# Replace with your EC2 instance public IP or domain
VITE_API_URL=http://your-ec2-ip:8156
VITE_API_BASE_URL=http://your-ec2-ip:8156/api/v1

# Redis Configuration (Optional)
REDIS_HOST=redis
REDIS_PORT=6379

# Logging
LOG_LEVEL=info

# AWS Configuration
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_S3_BUCKET=certificate-verification-documents

# AWS Service Enablement
AWS_TEXTRACT_ENABLED=true
AWS_TEXTRACT_MOCK_MODE=false
AWS_BEDROCK_ENABLED=true
AWS_BEDROCK_MOCK_MODE=false
AWS_BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0

# ============================================================================
# AWS RDS SETUP NOTES
# ============================================================================
# 1. Create RDS PostgreSQL instance in AWS Console
# 2. Configure security group to allow inbound traffic on port 5432 from EC2
# 3. Copy the RDS endpoint and update DB_HOST above
# 4. Use the master username and password you set during RDS creation
# 5. Ensure your EC2 instance can reach the RDS instance (same VPC or proper routing)
# ============================================================================